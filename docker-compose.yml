services:
  remote-claude:
    build: .
    restart: unless-stopped
    ports:
      - "9800:9800"
    volumes:
      - /home/nick:/home/nick
    environment:
      - HOME=/home/nick
      - NODE_ENV=production
      - DB_PATH=/app/data/remote-claude.db
