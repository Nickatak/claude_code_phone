# Pocket Claude — `spike` branch

Throwaway TS code against the Claude Agent SDK to test the design in [docs/DOMAIN_MODEL.md](docs/DOMAIN_MODEL.md). SDK term definitions live in [docs/GLOSSARY.md](docs/GLOSSARY.md).

This branch is intentionally minimal — only docs and the TS toolchain. Not for production. Will be reset, rewritten, or discarded depending on what the spike learns.

## Run a spike

```
npm install
node main.js
```
